# 多层级文档工程体系 v1.0

> 一句话：把"给 AI 编码 Agent 用的工程规格"组织成 **10 层有明确输入输出契约的生产线**——每层解决一类信息衰减，上一层是下一层的唯一输入源，治理层反向检查全链。

---

## 0. 这套体系解决什么问题

AI 编程助手的失败，很少是"写不出代码"，而是 **信息在传递中衰减**：

| 衰减现象 | 典型表现 |
|---------|---------|
| 技术栈漂移 | 定的是 PostgreSQL，写着写着变成 SQLite；文档 A 说 Redis，文档 B 说内存字典 |
| 数字漂移 | 需求说 JWT 30 分钟过期，接口规格写成 15 分钟；一处说 15 个工具，另一处说 16 个 |
| 引用断裂 | 引用了 `API-SPEC.md`，但仓库里根本没有这个文件（真实文件叫 `ADI.md`） |
| 全局不一致 | 每份文档单独看都成立，合起来互相矛盾；60 份文档没人读得完，矛盾就留到运行时 |
| 落地真空 | 有架构有需求，但没人写"这个函数具体怎么实现"→ AI 只能写出能跑不能用的空壳 |

这些问题的共同点：**不是能力问题，是缺少检查点**。人在传递信息时会自动纠偏，AI 不会——它只认当前上下文。

体系的解法：**在信息传递的每个必经节点放一层文档**，每层都有明确的输入、输出、消费方。层数不是拍脑袋定的，是"信息衰减点"的数量决定的。

---

## 1. 分层依据：为什么是 10 层

分层的原则只有一条：**每一层对应一类会在传递中丢失的信息，且丢失后无法在下游补救。**

| 层 | 防的是哪类衰减 | 如果这层缺失 |
|:--:|--------------|------------|
| 1 锚定 | 技术栈漂移 | 每份文档各选各的技术，最后无法集成 |
| 2 需求 | 目标漂移（做出来的不是要的） | 架构凭空设计，验收没有标准 |
| 3 架构 | 结构漂移（模块边界含糊） | 代码散装，改一处崩三处 |
| 4 工程 | 接口漂移（字段名/错误码/表结构对不上） | 前后端联调时才发现对不上 |
| 5 编码规格 | 实现真空（没人写函数级细节） | AI 写出空壳函数，逻辑留白 |
| 6 约束 | 判据真空（"哪里算错"说不清） | 审查全凭感觉，争议无法收敛 |
| 7 优化 | 只做完不迭代 | 上线即终点，问题不回流 |
| 8 治理 | 全局不一致 | 单份都对，合起来矛盾，交付后爆雷 |
| 9 交付 | 找不到入口 | 60 份文档没有导航，等于没有 |
| 10 运维 | 交付后失能 | 上线跑不起来 / 出故障没法修 |

**关键设计：第 1 层和第 6 层是"横向层"，被其他所有层消费。**

- 锚定层（第 1 层）的输出是**门禁判据**——后面每一层写完后都要拿它扫一遍，扫出违规就是缺陷。
- 约束层（第 6 层）的输出是**审查判据**——治理层（第 8 层）扫描时，判定"这是不是矛盾"的标准来自这里。

其余 8 层是"纵向层"，一级级往下传，构成主线。

---

## 2. 层间契约（本体系的核心）

每层只做三件事：**接收上游输出 → 加工成自己的产出 → 交付给下游消费方**。下面是完整契约表：

| # | 层 | 输入（来自） | 输出 | 消费方 | 缺失后果 |
|:-:|----|------------|------|--------|---------|
| 1 | 锚定 | 用户需求 + 环境审计（能不能 Docker / 什么数据库 / 有没有 K8s） | 技术约束清单（必须用 / 禁止用）+ 系统自我定位 + 关键参数表 | **全部层**（当门禁判据） | 技术栈漂移，文档之间互相矛盾 |
| 2 | 需求 | ① 的约束清单 | 功能清单 + 量化验收标准 + 用户流程 + 关键成功指标 + 范围边界 | 3 架构层、4 工程层、测试层 | 架构凭空设计；验收标准缺失，做完了不知道算不算完成 |
| 3 | 架构 | ② 的功能清单 | 模块清单 + 模块边界 + 技术决策记录（ADR：为什么选它不选那个） | 4 工程层、5 编码规格 | 有了需求没有骨架，代码无法拆分与协作 |
| 4 | 工程 | ③ 的模块清单 | 接口规格 / 数据模型（DDL + 索引）/ 错误码目录 / 日志规范 / 配置项清单 | 5 编码规格、6 约束层、10 运维层 | 每个模块各写各的，字段名和错误码对不上 |
| 5 | 编码规格 | ③ 模块 + ④ 接口/数据结构 | 每个代码模块一份规格：函数签名 + 参数校验顺序 + 伪代码 + 异常表 + 关联测试编号 | **AI 编码 Agent**（直接输入） | AI 得回头翻 20 万字需求文档，翻不动就自由发挥 |
| 6 | 约束 | ① 锚定 + 全层现状 | 逐维度独立成册的判据清单（每份 ≥5KB，含"如果 X 失败了怎么办"） | 8 治理层（审查时当判据） | 审查没有标准，"这算不算问题"无法收敛 |
| 7 | 优化 | ③ 架构 + ④ 工程现状 | 优化项：根因 + 改法 + 量化效果 + 验证方法 | 回灌 ②③（下一版迭代） | 只交付不迭代，踩过的坑不回填 |
| 8 | 治理 | **全部层** | 矛盾清单（源文档 + 目标文档 + 描述 + P0/P1/P2 + PASS/FAIL）+ 关键发现 + 质量门禁报告 | 打回对应层修复 | 单份都对、合起来矛盾，交付后爆雷 |
| 9 | 交付 | 全部层的产物 | 导航入口（每份文档的大小 + 用途）+ 可浏览文档站 | 人（新人、面试官、甲方） | 60 份文档找不到入口，等于没写 |
| 10 | 运维 | ④ 接口/配置 + ③ 架构 | 操作流程（6 大类）+ 故障场景处置（症状→诊断→修复→验证）+ 发布检查清单 + 排障指南 | 运维/接手的人 | 上线跑不起来，出故障靠猜 |

**契约的三条铁律**：

1. **上一层是下一层的唯一输入源。** 不允许"我凭经验补一个"——补的内容必须回流到上游层，否则就是漂移的起点。
2. **横向层（1、6）的产出必须被反复引用。** 锚定层写完后，后面每一层落盘前都要拿它扫一遍禁止词；约束层写完后，治理层的每条判定都要能指回某一份约束。
3. **治理层不可跳过。** 它的作用不是"再检查一遍"，而是唯一一个能看见全局的层。没有它，前 7 层的正确性都是局部正确。

---

## 3. 逐层说明

### 第 1 层｜锚定（3 份）

| 文档 | 是什么 | 干什么用 |
|------|--------|---------|
| 技术锚定 | 技术红线清单：必须用 / 禁止用什么 | 全流程的**门禁基准**。它写"禁止 Redis"，任何文档里冒出 Redis 就是 P0 违规。不写它，AI 会一路自己发明技术栈 |
| 系统定位 | 我是谁：定位、边界、不做什么 | 防跑偏。长流程最容易发生"写着写着变成另一个产品"，这份是每轮派生任务时重复注入的定盘星 |
| 参数锚定 | 从需求文档里抠出的关键数字表（超时 30 分钟、密码 ≥10 位…） | 防数字漂移。不同执行单元各写各的，30 分钟 vs 15 分钟是高频事故，这份让所有文档抄同一套数 |

**判据形态**：一张"必须用 / 禁止用"的双列表 + 一张关键数字表。这两张表会被下游反复引用。

### 第 2 层｜需求

**主文档**：产品需求文档（PRD）——市场背景、价值主张、商业模式、关键成功指标、功能清单、量化验收标准、用户流程、范围边界（做什么/不做什么）、竞品对比、演进路线。

**为什么需要"量化验收标准"**：功能清单只说了做什么，没说什么算做完。"支持退款"不是验收标准，"退款在 3 个工作日内到账，失败自动重试 3 次并告警"才是。

**大型项目补充**：需求层放不下的重量级论证（竞品深度分析、威胁建模、失效模式分析、基准测试数据、合规要求）独立成附录，避免主文档被稀释。

### 第 3 层｜架构（4 份）

| 文档 | 是什么 | 干什么用 |
|------|--------|---------|
| 架构决策记录 | 每条关键决策：背景 → 备选方案对比 → 决定 → 后果 | 回答"为什么选它而不是别的"。半年后有人问"当初为什么不用 Elasticsearch"，翻 ADR 就有答案 |
| 实现规格 | 每个模块的职责、内部结构、状态机、核心流程、错误处理、依赖 | 给写代码的人（或 AI）看的施工图。只有架构没有它，AI 只能写空壳 |
| 部署与运行 | 部署配置、数据流时序、错误模式目录、多租户隔离 | 回答"这套东西怎么跑起来、跑起来之后数据怎么流" |
| 架构全景图 | 一张图 + 映射表（功能→模块→关键表→缓存键）+ "为什么需要这套架构" | 迷路时先看它。文档一多，一张图能定位到该翻哪份 |

### 第 4 层｜工程（8~10 份）

| 文档 | 是什么 | 干什么用 |
|------|--------|---------|
| 接口规格 | 全部端点的完整定义 + 客户端集成配置 | 前端、第三方对接、写 SDK 的人按它来 |
| 数据模型 | 实体关系图 + 建库语句 + 索引策略 | 建库直接照它抄；索引策略决定系统快不快 |
| 错误码目录 | 错误码分类 + HTTP 状态映射 + 恢复策略 | 前后端联调时查它；保证同一类错误全系统用同一个码 |
| 日志规范 | 格式、级别、采样策略、链路追踪 ID | 出故障时唯一能救命的东西。没它，线上出事只能靠猜 |
| 配置清单 | 所有配置项 + 环境差异 | 换环境部署时逐项核对，防漏配 |
| 测试规格 | 分层策略 + 具体用例（含输入/预期输出）+ 边界值表 | 定义"什么叫测过了"，是验收依据 |
| 提示词库 | 全部 LLM 提示词集中管理（AI 项目必备） | 改提示词不用翻代码 |
| 专项手册 | 查询模式、缓存策略、性能基准、安全审计、前端模板、术语表、架构操作手册 | 各自领域的深水区手册，按项目类型增减 |

### 第 5 层｜编码规格

**每个代码模块一份**，内容包括：模块职责 → 文件结构 → 数据校验定义 → **每个函数的完整签名 + 参数校验顺序 + 具体 SQL / 缓存操作 / 审计调用 + 异常表 + 关联测试编号**。

判断标准只有一条：**AI 只读这一份就能写出模块，不用回头翻需求文档。** 做不到，就说明这层还缺内容。

这是整套体系里"最接近代码"的一层，也是 10 层中唯一直接面向执行者的层（其余 9 层都是给人看的推理与判据）。

### 第 6 层｜约束（10~11 份）

按维度**逐一独立成册**，不允许合并成一份大杂烩。每份要求：

- 体量下限（≥5KB，否则内容必然空洞）
- 章节数下限（≥5 个）
- **至少含 1 个"如果 X 失败了怎么办"的深度场景**（≥3 段展开）

产品型项目的典型 10 个维度：基础设施硬约束 / 安全 / 支付 / 隐私合规 / 国际化 / 接口版本化 / 监控 / 测试策略 / 编码规范 / 前后端一致性。

引擎型项目换成：嵌入模型 / 检索质量 / 分块策略 / 模型调用与降级 / 成本 / 故障模式 / 前沿基准 / 多模态 / 安全 / 隐私 / 硬约束。

**用途一句话**：它们是审查时的判据。治理层扫出来的每条矛盾，判定标准都必须来自这 10 份中的某一份。

### 第 7 层｜优化（全链路体检）

按**链路**拆分（认证链路 / 支付链路 / 数据链路 / 安全链路…），每条链路的每个优化项必须写全三件事：

1. **根因**——为什么要改（不是"这里可以更好"）
2. **改法**——具体步骤、参数、涉及文件
3. **量化效果**——改完预期提升多少（比例 / 延迟 / 成本）

只写一行编号的"优化点"等于没写——因为它无法执行，也无法验证。

### 第 8 层｜治理

| 文档 | 是什么 | 干什么用 |
|------|--------|---------|
| 矛盾矩阵 | 跨文档扫描结果（≥30 条）+ P0/P1/P2 分级 + PASS/FAIL + 修复后状态 | 交付前的质检报告。**有一条 P0 就不建议交付** |
| 关键发现 | 架构发现 / 产品发现 / 技术风险 | 沉淀"为什么最终是这个方案"，也是给下一轮的人的备忘 |
| 变更记录 | 版本变更记录 | 追"哪一版改了什么" |
| 版本归档 | 旧版留档 | 改坏了能翻回上一版 |

**矛盾矩阵的强制扫描项**（这是体系的"自动纠偏"机制）：

- 禁止词扫描：锚定层说禁止的技术，是否出现在其他文档里（排除"对比/竞品"语境）
- 跨文档数字一致性：同一指标在不同文档里是否一致（工具数、端点数量、超时时间）
- 引用有效性：文档正文引用的每个文件名是否真实存在（**高频事故**：派生任务自创了不存在的文件名）
- 体量声明准确性：导航页声称的大小与实际是否相符
- 技术选型一致性：需求 / 架构 / 实现规格里的技术栈描述是否一致

### 第 9 层｜交付

- **导航入口**：每一份文档一行，标注文件名 + 体量 + 一句话用途 + 处于哪一层。要求体量与真实文件偏差 ≤5%（写了不刷新会过时）
- **可浏览文档站**：把几十份 Markdown 生成可搜索的静态页面，双击即开、零依赖

### 第 10 层｜运维

| 文档 | 用途 |
|------|------|
| 操作流程 | 6 大类：环境搭建 / 日常开发 / 测试 / 发布 / 管理操作 / 故障恢复，每类 ≥8 段具体步骤 |
| 故障处置 | ≥6 个场景，每个写"症状 → 诊断 → 修复 → 验证"四段 |
| 发布检查清单 | 发布前检查 → 发布步骤 → 回滚方案 → 发布后 24 小时监控 |
| 环境/开发/排障/迁移指南 | 换台电脑、来了新人、升级数据库时的操作指南 |
| 容量规划 | 增长预测与扩容阈值 |

---

## 4. 项目类型适配

层数不变，**层的厚度和具体维度变**：

| | 引擎型 / Agent 型 | 产品型 |
|---|---|---|---|
| 需求层 | 主文档 + 5 份大附录（竞品/威胁建模/基准数据/数据模型/安全合规） | 主文档一份吃下（可到 20 万字级） |
| 约束层 | 11 份：嵌入 / 检索 / 分块 / 模型调用 / 成本 / 故障模式 / 前沿基准 / 多模态 … | 8~10 份：支付 / 合规 / 国际化 / 接口版本化 / 监控 / 前后端一致性 … |
| 架构层 | 实现规格按模块拆成多份分册 | 实现规格用"模块化格式"（模块职责→数据结构→核心流程→错误处理），伪代码留给编码规格层 |
| 编码规格层 | 函数级伪代码，含状态机 | 函数级签名 + SQL/缓存操作明细 |
| 自然体量 | 1.5MB 级（45~75 份） | 300~500KB 级（50~60 份） |
| 特有文档 | 功能规格目录、需求追溯矩阵、记忆策略、提示词工程、渗透测试报告 | 术语表、架构操作手册、前端模板规格、缓存策略、查询模式、性能基准 |

**体量不是目标，是结果。** 体系里没有任何一条规则要求"总字节数达到多少"——体量由"设计决策的总数"决定（每个决策约 750 字节），项目复杂就自然大。为了凑体量而批量生成表格数据，属于反模式。

---

## 5. 质量门禁（体系怎么自我验证）

每层落盘前后都有门禁，共 5 道：

| 门禁 | 检查什么 | 不通过怎么办 |
|------|---------|------------|
| ① 前置门禁 | 该层所有文件是否落盘、体量是否达标 | 退回重写（最多 2~3 轮） |
| ② 判据门禁 | 拿锚定层的约束清单扫全层，禁止词是否出现 | 出现即为 P0，修完才落盘 |
| ③ 深度门禁 | 随机抽 3 个段落，数"设计决策数量 ÷ 字节数"，低于该层类型基线 50% → 判定为"虚胖" | 退回重写，不允许靠字数凑 |
| ④ 一致性门禁 | 治理层跨文档扫描（5 类扫描项） | P0 全修 / P1 全修 / P2 修 ≥50%，最多 2 轮 |
| ⑤ 交付门禁 | 导航页体量声明 vs 实际文件、引用有效性 | 偏差 >5% 整行重写；断裂引用重映射到真实文件 |

**深度门禁是本体系区别于"文档生成工具"的关键。** 体量可以靠模板批量灌水，深度不能——深度门禁数的是"这段文字里有多少个真实决策"（例：一个函数的异常表算 3~4 个决策，一段废话算 0 个）。

---

## 6. 实测数据

体系已在 4 个项目上完整跑通（其中 2 个是客户项目，仅列规模不列内容）：

| 项目 | 类型 | 交付规模 | 构成 |
|------|:---:|:---:|------|
| 项目 A（开源，MCP 多模态记忆服务器） | 引擎型 | 72 份 / 1.5MB | 附录 ×6 + 约束 ×11 + 全链路优化 ×7 + 编码规格 ×10 |
| 项目 B（开源，直播智能总结工具） | 工具型 | 52 份 / 314KB | 约束 + 编码规格 + 运维手册全套 |
| 项目 C（客户项目） | 产品型 | 58 份 / 503KB | 约束 ×10 + 全链路优化 ×6 + 编码规格 ×12 |
| 项目 D（客户项目） | 产品型 | 61 份 / 1.5MB | 约束 ×10 + 全链路优化 ×5 + 编码规格 ×18 |

**迭代记录**：体系本身经过 29 个版本迭代，主要修正来自"产出物被复查时暴露的盲区"——例如早期版本导航页不刷新导致体量声明过时、约束文档被合并成一份导致审查无判据、优化项只有一行编号导致无法执行。**每次修正都固化回体系，而不是只修当次产物。**

---

## 7. 复用手册（新项目怎么套）

```
1. 复制 template/ 到新项目目录
2. 第 1 层起步：先填锚定（技术约束 + 定位 + 参数表）——不要跳过，它是所有层的判据源
3. 自上而下逐层推进：2 需求 → 3 架构 → 4 工程 → 5 编码规格
4. 第 6 层约束可与其他层并行（它的输入是第 1 层 + 各层现状）
5. 全部层落盘后跑 gate.py（本地门禁扫描脚本）
6. 第 8 层治理：全量扫描 → 出矛盾矩阵 → 打回对应层修复 → 复扫直至 P0=0
7. 第 9 层交付：生成导航页 + 文档站
```

**并行约束**：层内可并行（同一层多份文档同时写），层间必须串行（下游依赖上游产出）。违反这条，就会出现"架构文档里的模块清单和需求里的功能对不上"。

---

## 8. 反模式

| 不要 | 要 |
|------|-----|
| 跳过锚定层直接写需求 | 先定技术红线与参数表，所有层都拿它当判据 |
| 把 10 个约束维度合并成一份 | 逐维度独立成册，否则审查时找不到判据 |
| 优化项只写一行编号 | 每项写全"根因 + 改法 + 量化效果 + 验证方法" |
| 用字节数判断文档质量 | 数设计决策密度，低于该层基线 50% 判为虚胖，退回重写 |
| 为了总量好看批量生成表格数据 | 体量由决策数决定，不设 KPI；参考数据与设计文档分开管理 |
| 每份文档单独检查就交付 | 必须有能看见全局的治理层，否则正确性都是局部的 |
| 派生任务自创文件名 | 引用前先确认文件真实存在；断裂引用重映射到真实文件，绝不新建重复文档凑数 |
| 导航页写完不刷新 | 交付前重新统计实际体量与份数，偏差 ≤5% |
| 下游发现上游信息缺失时自己补 | 回流到上游层补，否则该信息在体系里没有归属，下次还会丢 |
| 层间并行推进 | 层内并行、层间串行——下游依赖上游产出 |
| 缺什么检查点就加一层文档 | 先判断它归哪一类衰减，能并入现有层的就并入，层数越多维护成本越高 |

---

## 附：体系速查

| 时机 | 看这些层 |
|------|---------|
| 项目启动 | 1 锚定 → 2 需求 |
| 设计阶段 | 3 架构（为什么这么选）→ 架构全景图（全局）→ 实现规格（怎么做） |
| 写代码 | 5 编码规格 → 4 工程层（数据模型 / 接口 / 配置）→ 6 约束（编码规范） |
| 写前端 | 前端模板规格 → 接口规格 → 前后端一致性约束 |
| 测试 | 测试规格 → 集成测试 → 测试策略约束 → 7 优化层 |
| 上线 | 发布检查清单 → 操作流程 → 部署/运维手册 |
| 出故障 | 故障处置（症状→诊断→修复→验证）→ 日志规范 → 排障指南 |
| 交付/面试 | 导航入口 → 需求主文档 → 关键发现 → 矛盾矩阵 → 优化层 |

**一句话概括各层分工**：锚定层定规矩，需求层定目标，架构层定骨架，工程层定零件，编码规格层定到函数级，约束层定红线，优化层定质量，治理层定可信度，交付层定入口，运维层定生存。
